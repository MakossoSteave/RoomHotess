import { GenremoviePipe } from './genremovie.pipe';

describe('GenremoviePipe', () => {
  it('create an instance', () => {
    const pipe = new GenremoviePipe();
    expect(pipe).toBeTruthy();
  });
});
